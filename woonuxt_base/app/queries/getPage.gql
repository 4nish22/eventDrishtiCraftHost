query getPage($slug: ID!) {
  page(id: $slug, idType: URI) {
    id
    title
    content
    slug
    date
    modified
    author {
      node {
        name
      }
    }
    featuredImage {
      node {
        sourceUrl
        altText
        title
      }
    }
  }
}
