query getProductCategories($first: Int = 20) {
  productCategories(first: $first, where: { orderby: TERM_ORDER, order: ASC, hideEmpty: false, parent: 0 }) {
    nodes {
      ...ProductCategory
    }
  }
}
